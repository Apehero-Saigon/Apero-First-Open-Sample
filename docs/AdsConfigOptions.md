Ads config full options
==================

```kotlin
val adsConfig = AperoFOAdsConfig.Builder()
    // Set inter splash high id
    .setInterSplashHighId(BuildConfig.inter_splash_high)
    // Set inter splash medium id
    .setInterSplashMediumId(BuildConfig.inter_splash_medium)
    // Set inter splash all price id
    .setInterSplashId(BuildConfig.inter_splash)
    
    // Set banner splash id
    .setBannerSplashId(BuildConfig.banner_splash)
    
    // Set native language high id
    .setNativeLanguageHighId(BuildConfig.native_language_high)
    // Set native language medium id
    .setNativeLanguageMediumId(BuildConfig.native_language_medium)
    // Set native language all price id
    .setNativeLanguageId(BuildConfig.native_language)
    
    // Set native language dup high id
    .setNativeLanguageDupHighId(BuildConfig.native_language_dup_high)
    // Set native language dup medium id
    .setNativeLanguageDupMediumId(BuildConfig.native_language_medium_high)
    // Set native language dup all price id
    .setNativeLanguageDupId(BuildConfig.native_language_dup)
    
    // Set native welcome high id
    .setNativeWelcomeHighId(BuildConfig.native_welcome_high)
    // Set native welcome medium id
    .setNativeWelcomeMediumId(BuildConfig.native_welcome_medium)
    // Set native welcome all price id
    .setNativeWelcomeId(BuildConfig.native_welcome)
    
    // Set native welcome dup high id
    .setNativeWelcomeDupHighId(BuildConfig.native_welcome_dup_high)
    // Set native welcome dup medium id
    .setNativeWelcomeDupMediumId(BuildConfig.native_welcome_medium_high)
    // Set native welcome dup all price id
    .setNativeWelcomeDupId(BuildConfig.native_welcome_dup)
    
    // Set native onboard screen 1 high id
    .setNativeOnboard1HighId(BuildConfig.native_onboard_1_high)
    // Set native onboard screen 1 medium id
    .setNativeOnboard1MediumId(BuildConfig.native_onboard_1_medium)
    // Set native onboard screen 1 all price id
    .setNativeOnboard1Id(BuildConfig.native_onboard_1)

    // Set native onboard screen 2 high id
    .setNativeOnboard2HighId(BuildConfig.native_onboard_2_high)
    // Set native onboard screen 2 medium id
    .setNativeOnboard2MediumId(BuildConfig.native_onboard_2_medium)
    // Set native onboard screen 2 all price id
    .setNativeOnboard2Id(BuildConfig.native_onboard_2)

    // Set native onboard screen 3 high id
    .setNativeOnboard3HighId(BuildConfig.native_onboard_3_high)
    // Set native onboard screen 3 medium id
    .setNativeOnboard3MediumId(BuildConfig.native_onboard_3_medium)
    // Set native onboard screen 3 all price id
    .setNativeOnboard3Id(BuildConfig.native_onboard_3)

    // Set native onboard screen 4 high id
    .setNativeOnboard4HighId(BuildConfig.native_onboard_4_high)
    // Set native onboard screen 4 medium id
    .setNativeOnboard4MediumId(BuildConfig.native_onboard_4_medium)
    // Set native onboard screen 4 all price id
    .setNativeOnboard4Id(BuildConfig.native_onboard_4)

    // Set native onboard screen full screen high id
    .setNativeOnboardFullscreenHighId(BuildConfig.native_ob_fullscr_high)
    // Set native onboard screen full screen medium id
    .setNativeOnboardFullscreenMediumId(BuildConfig.native_ob_fullscr_medium)
    // Set native onboard screen full screen all price id
    .setNativeOnboardFullscreenId(BuildConfig.native_ob_fullscr)

    .build()
```